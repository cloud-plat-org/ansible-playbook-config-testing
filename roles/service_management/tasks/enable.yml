---
- name: Enable service
  command: systemctl enable {{ service_name }}
  register: enable_result
  ignore_errors: true

- name: Show enable result
  debug:
    msg: "Enable result: {{ enable_result.stdout if enable_result.stdout else 'No output' }}, RC: {{ enable_result.rc }}"
  when: debug_mode | default(false)
